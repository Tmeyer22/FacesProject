Toby Meyer 10850762
Python in a notebook
Not really structured just runs from top down.
To run open this folder in an IDE like VSCode that can run python notebooks and run the notebook file, if the notebook is run outside of the folder make sure to update the path to the faces folder.